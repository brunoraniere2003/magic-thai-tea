# Spec 020 — WebGL Runtime · Tasks

## T1 — Deps + gate
- [ ] Instalar three/r3f/drei; `shouldRender3D` + teste.

## T2 — Runtime (`webgl/core/`)
- [ ] `R3FCanvas`, `Stage3D`, `useInViewActive`, `useDriveProgress`.

## T3 — Lenis+pin
- [ ] `normalizeScroll` no `MotionProvider`; não-regressão dos reveals/e2e.

## T4 — Demo + prova
- [ ] Rota dev guardada + cubo-demo gated (gira no scroll / poster no fallback); verificação real.

## T5 — Fechamento
- [ ] lint+typecheck+test+build; changelog; checkpoint do dono.
